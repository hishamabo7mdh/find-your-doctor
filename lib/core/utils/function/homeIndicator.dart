import 'package:flutter/material.dart';

Container homeIndicator() {
  return Container(
    width: 120,
    height: 5,
    decoration: BoxDecoration(
      color: Colors.black,
      borderRadius: BorderRadius.circular(10),
    ),
  );
}